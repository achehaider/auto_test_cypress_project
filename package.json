{
  "name": "hiboo-frontend-auto-tests",
  "version": "1.0.0",
  "description": "",
  "main": "",
  "devDependencies": {
    "@types/node": "^16.11.1",
    "cypress": "^8.6.0",
    "cypress-xpath": "^1.6.2",
    "typescript": "^4.4.4"
  },
  "scripts": {
    "cy:open": "cypress open",
    "cy:run": "cypress run",
    "cy:run:record": "cypress run --record",
    "cy:run:chrome": "cypress run --browser chrome",
    "cy:run:record:chrome": "cypress run --record --browser chrome",
    "cy:run:record:parallel": "cypress run --record --parallel",
    "cy:run:report": "cypress run --reporter cypress-multi-reporters --reporter-options configFile=reporter-config.json",
    "cy:verify": "cypress verify",
    "cy:info": "cypress info",
    "cy:version": "cypress version",
    "cy:cache:list": "cypress cache list"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/achehaider/auto_test_cypress_project.git"
  },
  "dependencies": {
    "cypress-multi-reporters": "^1.5.0",
    "mocha-junit-reporter": "^2.0.2"
  }
}
